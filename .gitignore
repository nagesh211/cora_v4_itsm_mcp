# Environments
.env
.venv
env/
venv/
ENV/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Pytest
.pytest_cache/
pytest-cache-files-*/
.coverage
htmlcov/

# Distribution / packaging
dist/
build/
*.egg-info/
.eggs/

# IDEs / Editors
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
models/.model_config*.yaml

# Deployment-specific operational scripts and seed data. Each site has its own
# OpenSearch cluster, its own KPI configs and its own module vocabulary, so none
# of this belongs in the repo — the server reads it all back from OpenSearch at
# runtime (see cora_mcp/module_registry.py).
load_module_meta.py
module_meta/